export default function Cart() {
    return (
        <div className="container py-5">
            <h2 className="fw-bold">Your Cart</h2>
            <p className="text-muted">Cart is empty. Add items from the shop to see them here.</p>
        </div>
    )
}
